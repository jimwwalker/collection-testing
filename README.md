Testing scripts for collections

* Requires cluster already created
* Setup dependencies - setup.sh
* . ./source.sh (but check the contents, paths/node etc...)
* Run a test e.g. bash tests/drop_collection.sh a.b c.d